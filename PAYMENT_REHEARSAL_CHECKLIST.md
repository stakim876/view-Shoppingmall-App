# 결제 리허설 체크리스트 (PortOne)

이 문서는 `MY Shop`의 결제 기능을 실제 배포 전에 안전하게 점검하기 위한 체크리스트입니다.

## 1) 사전 설정

- [ ] `client/.env`에 `VITE_PORTONE_STORE_ID` 실제 값 입력
- [ ] `backend/.env`에 `JWT_SECRET` 실제 값 입력
- [ ] 백엔드/프론트 재시작
  - [ ] `cd backend && npm start`
  - [ ] `cd client && npm run dev`
- [ ] 로그인 가능한 일반 사용자 계정 준비

## 2) 기본 동작 점검

- [ ] 로그인 후 장바구니에 상품 1개 이상 담기
- [ ] 체크아웃 화면 진입
- [ ] 이름/주소/연락처 입력
- [ ] 결제 수단(카드/네이버페이) 선택 가능 확인
- [ ] 결제 버튼 클릭 시 PortOne 창 정상 노출

성공 기준:
- 결제창이 뜨고, 에러 문구 `VITE_PORTONE_STORE_ID가 설정되지 않았습니다`가 보이지 않음

## 3) 성공 시나리오

- [ ] 결제 성공 후 `/api/orders` 200 응답 확인
- [ ] 응답에 `success: true`, `orderId` 존재 확인
- [ ] 결제 완료 페이지(`/order-complete`) 이동 확인
- [ ] 장바구니 비워짐 확인
- [ ] 주문 조회 화면에서 방금 주문 확인

성공 기준:
- 결제 성공 → 주문 저장 → 완료 페이지 이동이 한 흐름으로 이어짐

## 4) 실패 시나리오

### A. 결제 취소
- [ ] 결제창에서 취소
- [ ] 토스트로 실패/취소 안내 확인
- [ ] 주문이 생성되지 않았는지 확인

### B. 배송정보 누락
- [ ] 이름/주소/연락처 중 하나 비우고 결제 시도
- [ ] 프론트에서 안내 메시지 표시 확인

### C. 인증 없는 주문 시도
- [ ] 로그아웃 상태에서 결제 페이지 접근 후 결제 시도
- [ ] 로그인 페이지로 유도되는지 확인

### D. 쿠폰 금액 불일치
- [ ] 쿠폰 적용 후 금액 조작 시도(개발자도구 등)
- [ ] 서버가 `결제 금액이 일치하지 않습니다`로 차단하는지 확인

## 5) 운영 전 최종 보안 점검

- [ ] `JWT_SECRET`가 강한 랜덤값인지 확인
- [ ] `ADMIN_INVITE_CODE` 운영값으로 변경
- [ ] DB 계정/비밀번호가 기본값이 아닌지 확인
- [ ] CORS가 운영 도메인으로 제한되어 있는지 확인
- [ ] `.env`가 커밋되지 않았는지 확인

## 6) 장애 대응 체크

- [ ] 결제 실패 로그 위치 확인 (브라우저 콘솔 + 백엔드 로그)
- [ ] 주문 실패 시 사용자에게 안내 메시지 노출 확인
- [ ] 재시도 시 중복 주문이 생기지 않는지 확인

## 7) 빠른 진단 명령어

백엔드 헬스:

```bash
curl http://localhost:3001/api/health
```

기대 결과:

```json
{"ok":true,"message":"API 서버 정상"}
```

---

면접에서 한 줄로 설명:

> "결제는 성공/실패/취소/비정상 입력까지 시나리오 테스트하고, 서버에서 인증과 금액 검증으로 최종 방어합니다."
